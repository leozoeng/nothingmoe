# Supabase — NothingMoe org: https://supabase.com/dashboard/org/xvxzogbnivtgudqnfnfi
# Project Settings → API: copy URL + anon (legacy JWT) or publishable key
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_jwt_or_publishable_key

# Optional local-only admin key (never expose to client or commit real values)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Disable email confirmation + set redirect URLs (after CLI login):
# npx supabase link --project-ref bzycbpelzsddolghtysa && npx supabase config push --yes

# Site owners: insert into nothingmoe_site_owners after users sign up.
